<%= form_for post do |f| %>
    <div class="field">
        <%= f.label :content %> <br />
        <%= f.text_area :content %>
    </div>

    <div class="field">
        <%= f.submit "Create post" %>
    </div>
<% end %>